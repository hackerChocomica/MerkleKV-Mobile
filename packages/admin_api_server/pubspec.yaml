name: admin_api_server
version: 0.1.0
publish_to: none
description: Minimal admin dashboard API for MerkleKV Mobile (system stats + MQTT/log stream)

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  shelf_static: ^1.1.2
  merkle_kv_core:
    path: ../merkle_kv_core
  http_parser: ^4.0.2
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.8
  json_serializable: ^6.7.1
  test: ^1.25.2
